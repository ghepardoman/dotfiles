#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
alias grep='grep --color=auto'
PS1='[\u@\h \W]\$ '
eval "$(starship init bash)"
umask 002

# Created by `pipx` on 2024-03-14 16:20:56
export PATH="$PATH:/home/ghepa/.local/bin"
